## Why you used ArrayList instead of array
Array list in collection is not fixed size like Arrays. So, it can increase size based on input data user adds.
we can manipulate with different methods under Array list like remove(), add() etc.

## Where you used static members and why
static is used in ID generator since id generated is being used over all application that ensure uniqe IDs.

## Where you used inheritance and what you gained from it
Student class (Child class) that inherits Person (parent class)
that eliminates duplication of common field which can be used in both Student and Trainer
